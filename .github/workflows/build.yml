name: Build Dedicated Blade Runner ScummVM

on:
  push:
    branches: [ "zh-tw-bladerunner" ]
  pull_request:
    branches: [ "zh-tw-bladerunner" ]
  workflow_dispatch:
    
jobs:
  # ======== Windows 專用極簡建置 (UCRT64) ========
  build-windows:
    name: Build for Windows
    runs-on: windows-latest

    defaults:
      run:
        shell: msys2 {0}
     
    steps:
      - name: 檢出程式碼
        uses: actions/checkout@v4

      - name: 初始化 MSYS2 (UCRT64) 與安裝相依套件
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            base-devel
            git
            make
            mingw-w64-ucrt-x86_64-gcc
            mingw-w64-ucrt-x86_64-pkgconf
            mingw-w64-ucrt-x86_64-SDL2
            mingw-w64-ucrt-x86_64-freetype
            mingw-w64-ucrt-x86_64-harfbuzz
            mingw-w64-ucrt-x86_64-brotli
            mingw-w64-ucrt-x86_64-bzip2
            mingw-w64-ucrt-x86_64-zlib

      - name: 專用引擎配置與完全靜態編譯
        run: |
          export PKG_CONFIG_PATH=/ucrt64/lib/pkgconfig:/ucrt64/share/pkgconfig
          export PATH=/ucrt64/bin:$PATH
          export SDL_CONFIG=/ucrt64/bin/sdl2-config
          export LDFLAGS="-static-libgcc -static-libstdc++ -Wl,-Bstatic -lfreetype -lpng -lz -lbrotli -lharfbuzz -Wl,-Bdynamic"
          export CFLAGS="-Os"
          export CXXFLAGS="-Os"
    
          # Verify SDL2 is properly installed
          echo "===== SDL2 Verification ====="
          which sdl2-config
          sdl2-config --version
          sdl2-config --cflags
          sdl2-config --libs
          pkg-config --modversion sdl2
    
          ./configure \
              --disable-all-engines \
              --enable-engine=bladerunner \
              --enable-freetype2 \
              --disable-debug \
              --enable-static \
              --with-sdl-prefix=/ucrt64

      - name: Debug pkg-config
        shell: msys2 {0}
        run: |
          echo "===== pkg-config ====="
          which pkg-config
          pkg-config --version
          
          echo
          echo "===== freetype pkg-config (靜態) ====="
          pkg-config --libs --static freetype2
          pkg-config --libs --static harfbuzz
          pkg-config --variable pc_path pkg-config

          echo
          echo "===== config.mk ====="
          grep -i freetype config.mk || true
          grep -i harfbuzz config.mk || true
          grep -i brotli config.mk || true
          grep -i bz2 config.mk || true

      - name: Debug config.mk
        shell: msys2 {0}
        run: |
          grep "^FREETYPE" config.mk || true
          grep "^LIBS" config.mk || true
          grep "^LDFLAGS" config.mk || true
          grep "^DEFINES" config.mk || true
      
      - name: Build
        run: |            
          make V=1 -j1

      - name: Verify linking
        shell: msys2 {0}
        run: |
          echo "===== 檢查執行檔依賴 ====="
          ldd scummvm.exe || echo "檢查完成"
          echo
          echo "===== 執行檔大小 ====="
          ls -lh scummvm.exe
          echo
          echo "===== 詳細檢查：查詢 DLL 依賴 ====="
          objdump -p scummvm.exe | grep "DLL Name" || echo "無外部 DLL 依賴"

      - name: Strip & Optimize
        run: |          
          strip --strip-all --remove-section=.comment scummvm.exe
          echo "===== Strip 後大小 ====="
          ls -lh scummvm.exe

      - name: 打包 Windows 懶人包
        run: |
          mkdir scummvm-windows-zh-TW
          cp scummvm.exe scummvm-windows-zh-TW/
          if [ -d "packaging/common" ]; then cp -r packaging/common/* scummvm-windows-zh-TW/; fi
          if [ -d "packaging/windows" ]; then cp -r packaging/windows/* scummvm-windows-zh-TW/; fi

      - name: 上傳 Windows 成品
        uses: actions/upload-artifact@v4
        with:
          name: scummvm-windows-zh-TW
          path: scummvm-windows-zh-TW/

  # ======== Linux 專用極簡建置 ========
  build-linux:
    name: Build for Linux
    runs-on: ubuntu-latest
    
    steps:
      - name: 檢出程式碼
        uses: actions/checkout@v4

      - name: 安裝 Linux 系統相依套件
        run: |
          sudo apt-get update
          sudo apt-get install -y g++ make pkg-config libsdl2-dev libpng-dev libjpeg-dev libfreetype-dev zlib1g-dev libogg-dev libvorbis-dev libtheora-dev libflac-dev

      - name: 專用引擎配置與編譯瘦身
        run: |
          CXXFLAGS="-Os" ./configure \
            --disable-all-engines \
            --enable-engine=bladerunner \
            --enable-freetype2 \
            --disable-debug \
            --enable-static
          make -j$(nproc)
          strip scummvm

      - name: 打包 Linux 懶人包
        run: |
          mkdir scummvm-linux-zh-TW
          cp scummvm scummvm-linux-zh-TW/
          if [ -d "packaging/common" ]; then cp -r packaging/common/* scummvm-linux-zh-TW/; fi
          if [ -d "packaging/linux" ]; then cp -r packaging/linux/* scummvm-linux-zh-TW/; fi
          chmod +x scummvm-linux-zh-TW/scummvm scummvm-linux-zh-TW/runme.sh

      - name: 上傳 Linux 成品
        uses: actions/upload-artifact@v4
        with:
          name: scummvm-linux-zh-TW
          path: scummvm-linux-zh-TW/

  # ======== macOS 專用極簡建置 ========
  build-macos:
    name: Build for macOS
    runs-on: macos-latest
    
    steps:
      - name: 檢出程式碼
        uses: actions/checkout@v4

      - name: 安裝 macOS Homebrew 相依套件
        run: |
          brew install \
              sdl2 \
              pkg-config \
              freetype \
              libpng \
              jpeg \
              libogg \
              libvorbis \
              theora \
              flac \
              bzip2 \
              zlib  

      - name: Check SDL
        run: |
          which sdl2-config
          sdl2-config --version
          sdl2-config --cflags
          sdl2-config --libs

          pkg-config --modversion sdl2
          pkg-config --cflags sdl2
          pkg-config --libs sdl2

      - name: 專用引擎配置與編譯瘦身
        env:
          PKG_CONFIG_PATH: /opt/homebrew/lib/pkgconfig
          SDL_CONFIG: /opt/homebrew/bin/sdl2-config
        run: |
          ./configure \
            --disable-all-engines \
            --enable-engine=bladerunner \
            --enable-freetype2 \
            --disable-debug
          make -j$(sysctl -n hw.ncpu)
          strip scummvm

      - name: 打包 macOS 懶人包
        run: |
          mkdir scummvm-macos-zh-TW
          cp scummvm scummvm-macos-zh-TW/
          if [ -d "packaging/common" ]; then cp -r packaging/common/* scummvm-macos-zh-TW/; fi
          if [ -d "packaging/macos" ]; then cp -r packaging/macos/* scummvm-macos-zh-TW/; fi
          chmod +x scummvm-macos-zh-TW/scummvm scummvm-macos-zh-TW/runme.sh

      - name: 上傳 macOS 成品
        uses: actions/upload-artifact@v4
        with:
          name: scummvm-macos-zh-TW
          path: scummvm-macos-zh-TW/